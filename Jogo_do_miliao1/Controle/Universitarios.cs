using Controle;
using Jogo_do_miliao1;

public class Universitario : IAjuda
{
    public override void RealizaAjuda(Questao questao)
    {
        var.porcentagem=100;
        for (int i=0; i<5; i++)
        {
            int num Rand = 0;
            if (porcentagem > 0)
        {
            num RandRand = Radom.Shared.Next(0.porcentagem);
        }
        }
    }
}